class Basic{
    public static void main(String[] args){
        int num[] = {1,2,3,4,5}; // this is an array named arr, which holds 5 values from 1 to 5
        
        int num[] = new int[20];
        for(int i = 0; i < 3; i++){
            num[i] = sc.nextInt();
        }
        int len = num.length(); // if function, then just array name
        if num[4] = 5; // if condition or any other case, then put particular index of array
        // Index:    0,1,2,3,4 // array index always starts with 0
        char ch[] = {'a', 'b', 'c', 'd'};
        String str[] = {"hello", "world", "codes"};
        for(int i = 0; i < 3; i++){
            System.out.println(str[i]);
        }
    }
}